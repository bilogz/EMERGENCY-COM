<?php
// Copy this to mail_config.php and set values for SMTP
return [
    // SMTP server settings
    'host' => 'smtp.example.com',
    'port' => 587,
    'username' => 'smtp_user@example.com',
    'password' => 'smtp_password',
    'secure' => 'tls', // 'tls' or 'ssl' or '' (empty string)
    'auth' => true,

    // From address
    'from_email' => 'no-reply@emergency.com',
    'from_name' => 'Emergency Communication System',

    // Optional: override mail fallback when SMTP fails
    'send_fallback_to_mail' => true,
    'debug' => false
];
